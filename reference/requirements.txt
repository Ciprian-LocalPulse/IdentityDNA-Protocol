# IdentityDNA Protocol — reference implementation dependencies
# Install: pip install -r reference/requirements.txt --break-system-packages

cryptography>=42.0        # AEAD (ChaCha20-Poly1305), HKDF, HKDF-Expand
pynacl>=1.5                # Ed25519 signatures, X25519 ECDH
argon2-cffi>=23.1          # Argon2id (human-secret KDF only, RFC-0001 §11.1)
blake3>=1.0.9                # BLAKE3-256 hashing (falls back to SHA3-256 if absent)
fastapi>=0.110             # reference/server/api.py
uvicorn>=0.29               # ASGI server to run api.py
pydantic>=2.6               # request/response models for api.py
click>=8.1                  # cli/identitydna
pytest>=8.0                 # tests/unit, tests/integration
